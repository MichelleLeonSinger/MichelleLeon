import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  base: '/LANDING_PAGE_MICHELLE_LEON/', // cambia esto según tu repositorio
  plugins: [react()],
});
